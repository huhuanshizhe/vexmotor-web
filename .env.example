API_URL=http://localhost:5100
SITE_URL=http://localhost:5000

# Shared secret for admin → web cache revalidation (ui-strings). Set the same value in admin .env.
REVALIDATE_SECRET=replace-with-a-long-random-secret
